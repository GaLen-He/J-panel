# 更新日志

## v0.11.11 - 2026-07-27

### 新增

- 新增 `J 切割` 工具，可在编辑模式中用方框、圆形、折线、套索、贝塞尔和魔棒子工具绘制切割线，并通过投影方式一次性执行切割。
- 新增魔棒选区切割：点击带图像纹理的参考物体，根据颜色容差自动识别色块轮廓并生成闭合切割线，支持反选图案主体和平滑曲线描边。
- 新增曲线倒角功能，在曲线编辑模式下可用 `Ctrl+Shift+B` 交互式倒角，拖动鼠标调整半径，滚轮调整段数。
- 新增自动铺陈排列功能，可按行数或列数把选中物体在 XY 平面自动排布，并支持按父级、集合分组、排序、间距和底部贴地。
- 新增 `J Panel` 钉固为浮动窗口入口，可把完整面板打开成独立窗口，方便长期停留使用。
- 新增 `J 切割` 自定义工具图标资源 `quick_ops/icons/qops.cutter.dat`。

### 改进

- `N > J` 面板新增 `切割工具` 和 `自动排列` 分区，常用切割、排列参数可以直接在侧栏设置。
- 完整 J Panel 弹出面板中加入自动排列和钉固浮窗入口，常用功能集中度更高。
- 快捷键更新：`Alt+Shift+J` 呼出完整 J Panel 面板，`Alt+Shift+C` 激活 T 面板的 `J 切割` 工具，曲线编辑模式下 `Ctrl+Shift+B` 执行曲线倒角。
- 为切割线等模态工具补充 2D 屏幕绘制辅助，兼容 Blender 3.x 与 4.x 的内置着色器命名差异。

### 修正

- 自动铺陈排列的 redo/F9 参数可稳定回放，重新调整行列、间距、排序和贴地选项时会基于初始快照重排，避免受当前对象位置影响。
- 切割会话支持擦除、端点吸附、复制、整体移动/缩放/旋转、贝塞尔锚点编辑和逐级取消，减少复杂切割过程中的误操作成本。
## v0.8.3 - 2026-07-04

### 新增

- 新增 3DCoat 互导功能，可将 Blender 中选中的网格物体发送到 3DCoat，并从 3DCoat 取回更新后的模型。
- 新增 3DCoat AppLink 交换目录支持：可自动探测默认 Exchange 目录，也可在插件偏好设置中手动指定。
- 新增 3DCoat 互导参数：发送缩放、取回缩放、发送到世界原点、取回时清理无纹理空材质。
- 新增 J Panel 功能菜单，可一键呼出布尔、线框、镜像、3DCoat 互导等常用操作，便于配合 PieMenuEditor 或自定义调用。
- 新增完整弹出面板入口 `qops.show_panel`，可通过快捷键或 PieMenuEditor 呼出完整 J Panel 面板。

### 改进

- 在 3D 视图侧栏 `N > J` 中加入独立的 `3DCoat 互导` 面板，常用互导设置和发送/取回按钮可直接操作。
- 在插件偏好设置中加入 3DCoat 配置区，并保留快捷键列表，方便统一管理快捷键。
- 为 3DCoat 发送、取回和 J Panel 弹出面板注册快捷键条目，默认不占用按键，用户可自行绑定。
- 发送到 3DCoat 前会补齐基础 UV，并按 AppLink 机制写入握手文件，降低 3DCoat 端识别失败的概率。

### 修正

- 调整 3DCoat 往返缩放处理，发送和取回缩放可独立设置，避免模型在 3DCoat 与 Blender 之间来回导入导出后尺寸漂移。
- 取回模型后会清理触发文件，避免重复导入同一个 3DCoat 回传模型。

## v0.5.0 - 2026-07-02

- 初始发布 J Panel。
- 集成布尔运算体选择与显隐控制。
- 集成交互式镜像修改器操作。
- 集成线框显示物体显隐控制。
- 支持在插件偏好设置中管理默认快捷键。

